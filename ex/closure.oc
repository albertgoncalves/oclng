entry {
    let counter \ {
        let scope (alloc 2);
        seta scope 0 \scope {
            seta scope 1 (+ (get scope 1) 1);
            (get scope 1)
        };
        seta scope 1 0;
        scope
    };
    let scope0 (counter);
    let scope1 (counter);
    ((get scope0 0) scope0);
    ((get scope1 0) scope1);
    (printf "%ld\n" ((get scope0 0) scope0));
    0
}
