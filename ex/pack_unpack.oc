f x {
    unpack x {
        {
            let y (+ 1 2)
            (printf "first %d\n" y)
        }
        y {
            (printf "second %s\n" y)
        }
    }
    let y (- 1 1)
    y
}

_entry_ {
    (f (pack 0))
    (f (pack 1 "path"))
    0
}
